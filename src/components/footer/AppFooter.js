import React, { Component } from 'react';
import Footer from '../footer/compFooter/Footer'

export class AppFooter extends Component {
    render() {
        return(
            <footer>
                <Footer />
            </footer>
        )
    }
}

export default AppFooter;